#!/bin/sh

# Add Newbler, Last and Blat local copies to your PATH
PATH=
# Add Perl graph, GetOpt,Perl Set::IntSpan module to your PERL5LIB PATH
PERL5LIB=
# DO NOT MODIFY FROM THIS POINT !
PATH=`dirname $0`/:$PATH
export PATH

exec `basename $0`_wrapped "$@"
